# 实验室化学试剂管理系统 - 数据库设计说明

## 数据库逻辑结构设计
在数据库逻辑架构的构建过程中，通过定义数据对象的主标识符与外键约束等关联机制，构建结构化数据间的映射规则，以确保其完整性。为避免出现错误，需要考虑采用规范化设计（**加粗**表示主键，（外键）表示关联约束）：

### （1）系统用户关系模式
（**用户 id**、用户名、密码、真实姓名、角色、所属部门、邮箱、手机号、账号状态、创建时间、更新时间）

### （2）试剂分类关系模式
（**分类 id**、分类名称、分类编码、描述信息、创建时间、更新时间）

### （3）试剂基础信息关系模式
（**试剂 id**、名称、CAS 号、分类 id（外键）、规格型号、单位、危险等级预警、描述、创建时间、更新时间）

### （4）存放位置关系模式
（**位置 id**、房间名称、机柜号、货架号、完整位置路径、位置描述信息、创建时间、更新时间）

### （5）库存实体关系模式
（**库存 id**、试剂 id（外键）、位置 id（外键）、批次号、物料数量、预警阈值、有效期、供应商、采购日期、库存状态、备注信息、创建时间、更新时间）

### （6）领用申请关系模式
（**申请 id**、申请单号、试剂 id（外键）、申请数量、申请用途、申请人 id（外键）、审核状态、审核人 id（外键）、审核时间、审核备注、创建时间、更新时间）

### （7）入库记录关系模式
（**记录 id**、试剂 id（外键）、库存 id（外键）、位置 id（外键）、入库批号、入库数量、有效期、供应商、操作人 id（外键）、入库备注、创建时间）

### （8）出库记录关系模式
（**记录 id**、试剂 id（外键）、库存 id（外键）、申请单 id（外键）、出库数量、领用人 id（外键）、操作人 id（外键）、用途说明、出库备注、创建时间）

### （9）系统公告关系模式
（**公告 id**、标题、公告内容、发布对象、优先级、发布人 id（外键）、发布时间、创建时间、更新时间）

### （10）问题反馈关系模式
（**反馈 id**、用户 id（外键）、反馈类型、标题、反馈详情、处理状态、处理人 id（外键）、处理意见、处理时间、创建时间）

### （11）消耗预测关系模式
（**预测 id**、试剂 id（外键）、当前库存、预计耗尽日期、建议采购日期、建议订购量、AI 置信度、创建时间）

### （12）废弃记录关系模式
（**记录 id**、试剂 id（外键）、库存 id（外键）、废弃数量、处理方式、操作人 id（外键）、备注说明、创建时间）
