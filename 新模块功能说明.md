# 实验室管理系统 - 五大新模块功能说明

## 概述

本次更新为实验室管理系统添加了五个强大的新模块，涵盖安全合规、采购预算、生命周期追踪、废弃物管理和AI智能分析等功能，极大地提升了实验室管理的专业性和智能化水平。

---

## 模块一：安全与合规 (GHS & SOP) 模块

### 1.1 GHS/MSDS 结构化数据库

**功能描述**：
- 不再简单上传PDF文件，系统将MSDS关键信息结构化存储
- 自动提取GHS象形图、H-phrases（危险说明）、P-phrases（防范说明）
- 存储物理化学性质、急救措施、消防措施等完整信息

**数据表**：`ghs_msds`

**核心字段**：
- `ghs_pictograms`: GHS象形图（JSON数组）
- `h_phrases`: 危险说明（JSON数组）
- `p_phrases`: 防范说明（JSON数组）
- `signal_word`: 信号词（Danger/Warning）
- `hazard_class`: 危险类别
- `ai_extracted`: 是否AI提取（1/0）
- `verified`: 是否已验证（1/0）

**API接口**：
- `GET /ghs/reagent/{reagentId}` - 根据试剂ID获取MSDS
- `POST /ghs/save` - 保存或更新MSDS
- `GET /ghs/unverified` - 获取未验证的MSDS列表

**AI辅助功能**：
实验员上传MSDS文件后，可调用AI服务自动提取结构化信息，大幅减少录入负担。

---

### 1.2 智能存储不兼容警告

**功能描述**：
- 系统管理员可标记库位属性（通风、防爆、腐蚀性、易燃品区等）
- 存储试剂时自动检查不兼容性，阻止高危操作
- 记录所有警告日志供审计

**数据表**：
- `storage_location_attributes` - 库位属性表
- `storage_incompatibility_rules` - 存储不兼容规则表
- `storage_warning_log` - 存储警告日志表

**预设不兼容规则**：
1. 强氧化剂 ⚠️ 易燃品 - 高危
2. 强氧化剂 ⚠️ 还原剂 - 高危
3. 强酸 ⚠️ 强碱 - 高危
4. 强酸 ⚠️ 活泼金属 - 高危
5. 易燃液体 ⚠️ 氧化剂 - 高危

**API接口**：
- `POST /storage/compatibility/check` - 检查存储兼容性

**核心Service**：`StorageCompatibilityService.java`

**使用场景**：
```java
// 入库时检查兼容性
Map<String, Object> result = compatibilityService.checkCompatibility(
    reagentId, locationId, operatorId, operatorName
);

if (result.get("blocked")) {
    // 阻止操作，显示警告信息
    List<String> warnings = (List<String>) result.get("warnings");
}
```

**AI辅助功能**：
根据试剂的GHS信息，AI自动推荐最佳存储位置。

---

### 1.3 SOP 智能匹配与准入

**功能描述**：
- 管理员上传标准操作规程（SOP）
- 学生领用高危试剂时自动弹出相关SOP
- 强制完成安全培训小测试后才能领用
- AI随机从SOP中抽取问题进行考核

**数据表**：
- `sop_document` - SOP文档表
- `sop_training_record` - SOP培训与准入记录表

**核心字段**：
- `applicable_reagents`: 适用试剂（JSON数组）
- `applicable_hazard_types`: 适用危险品类型（JSON数组）
- `key_points`: 关键要点（JSON数组）
- `effective_date`: 生效日期
- `status`: 状态（DRAFT/ACTIVE/ARCHIVED）

**API接口**：
- `GET /sop/page` - 分页查询SOP
- `POST /sop/save` - 保存或更新SOP
- `GET /sop/training/check` - 检查培训状态
- `POST /sop/training/record` - 记录培训
- `GET /sop/training/user/{userId}` - 获取用户培训记录

**AI辅助功能**：
学生可以随时提问："处理[试剂名]时我该注意什么？"，AI基于SOP和MSDS进行回答。

---

## 模块二：采购与预算 (Procurement & Finance) 模块

### 2.1 课题组预算管理

**功能描述**：
- 创建不同的课题组/项目，分配预算
- 实验员和学生归属于某个课题组
- PI（负责人）可管理本组预算和审批

**数据表**：
- `research_group` - 课题组/项目表
- `research_group_member` - 课题组成员表

**核心字段**：
- `total_budget`: 总预算
- `used_budget`: 已使用预算
- `available_budget`: 可用预算
- `budget_year`: 预算年度

**API接口**：
- `GET /research-group/page` - 分页查询课题组
- `POST /research-group/save` - 保存或更新课题组
- `POST /research-group/member/add` - 添加成员
- `GET /research-group/member/list/{groupId}` - 获取成员列表

---

### 2.2 采购工作流

**功能描述**：
- 学生/实验员提交采购申请
- 库存不足时自动转为采购申请
- 审批流程：采购申请 → PI审批 → 管理员审批 → 下单 → 入库

**数据表**：`procurement_request` - 采购申请表

**状态流转**：
1. `PENDING` - 待审核
2. `PI_APPROVED` - PI已批准
3. `ADMIN_APPROVED` - 管理员已批准
4. `ORDERED` - 已下单
5. `RECEIVED` - 已收货
6. `REJECTED` - 已拒绝
7. `CANCELLED` - 已取消

**API接口**：
- `POST /procurement/create` - 创建采购申请
- `POST /procurement/approve/pi` - PI审批
- `POST /procurement/approve/admin` - 管理员审批（下单）
- `GET /procurement/page` - 分页查询采购申请

**核心功能**：
- PI审批时自动检查预算是否充足
- 下单时自动扣减预算
- 支持紧急程度设置（URGENT/NORMAL/LOW）

---

### 2.3 成本核算与分摊

**功能描述**：
- 每次领用操作实时扣减课题组预算
- 记录所有预算交易
- 支持按课题组、时间段、试剂类型进行成本分析

**数据表**：`budget_transaction` - 预算使用记录表

**交易类型**：
- `PROCUREMENT` - 采购
- `USAGE` - 使用
- `REFUND` - 退款
- `ALLOCATION` - 分配

**核心Service**：`ProcurementService.java` 和 `ResearchGroupService.java`

**AI辅助功能**：
- "帮我分析一下，第三季度A课题组在'酶'上的花费是多少？"
- "对比一下V牌和T牌的同类试剂，哪个性价比更高？"

---

## 模块三：全生命周期追踪 (Physical & Lifecycle) 模块

### 3.1 二维码/RFID 集成

**功能描述**：
- 所有入库试剂自动生成唯一二维码
- 标签包含：ID、名称、入库日期、有效期、位置
- 支持手机App扫码操作

**数据表**：
- `reagent_lifecycle` - 试剂生命周期记录表
- `scan_operation_log` - 扫码操作日志表

**二维码格式**：`QR{timestamp}_{inventoryId}`

**扫码操作**：
1. 扫库位码 → 显示该位置所有物品
2. 扫试剂码 → 显示详细信息，快捷操作（领用/转移/报废）
3. 学生领用 → 扫码确认，双向验证

**API接口**：
- `GET /lifecycle/scan/{qrCode}` - 扫码查询
- `POST /lifecycle/mark-opened` - 标记为已开封
- `POST /lifecycle/record-usage` - 记录使用
- `POST /lifecycle/transfer` - 转移库位

---

### 3.2 试剂状态跟踪

**功能描述**：
- 跟踪试剂物理状态（密封/已开封/部分使用/已用完/已损坏）
- 记录开封日期和开封后有效期
- 自动计算开封后失效日期

**物理状态**：
- `SEALED` - 密封
- `OPENED` - 已开封
- `PARTIALLY_USED` - 部分使用
- `EMPTY` - 已用完
- `DAMAGED` - 已损坏

**核心功能**：
```java
// 标记为已开封
lifecycleService.markAsOpened(qrCode, userId, userName, validityDays);

// 记录使用
lifecycleService.recordUsage(qrCode, usedQuantity);

// 转移库位
lifecycleService.transferLocation(qrCode, newLocationId, reason);
```

**位置历史记录**：
系统自动记录试剂的所有位置变更历史（JSON格式）。

---

### 3.3 可视化库存

**功能描述**：
- 绘制实验室平面图或柜子示意图
- 在图形界面上高亮显示物品位置
- 支持多种形状（矩形、圆形、多边形）

**数据表**：
- `storage_location_map` - 库位可视化配置表
- `storage_location_coordinate` - 库位坐标表

**地图类型**：
- `FLOOR_PLAN` - 平面图
- `CABINET` - 柜子图

**配置示例**：
```json
{
  "locationId": 1,
  "mapId": 1,
  "xCoordinate": 100,
  "yCoordinate": 200,
  "width": 50,
  "height": 30,
  "shape": "RECTANGLE",
  "color": "#FF5733"
}
```

---

## 模块四：废弃物管理 (Waste Disposal) 模块

### 4.1 废液/固废登记

**功能描述**：
- 试剂用空或实验产生废液时必须通过系统提交报废申请
- 登记废弃物类型、主要成分、数量
- 自动生成符合规范的危险废弃物标签

**数据表**：
- `waste_category` - 废弃物类别表
- `waste_disposal_record` - 废弃物登记表

**废弃物类型**：
- `LIQUID` - 废液
- `SOLID` - 固废
- `GAS` - 废气
- `EMPTY_CONTAINER` - 空容器

**预设类别**：
1. 含卤素有机废液（高危）
2. 不含卤素有机废液（中危）
3. 无机酸性废液（高危）
4. 无机碱性废液（高危）
5. 重金属废液（高危）
6. 实验固废（中危）
7. 空试剂瓶（低危）
8. 一般废弃物（无害）

**API接口**：
- `POST /waste/create` - 创建废弃物登记
- `GET /waste/categories` - 获取所有废弃物类别
- `POST /waste/update-status` - 更新状态
- `GET /waste/label/{id}` - 生成标签

**AI辅助功能**：
根据用户描述的成分（如"丙酮和乙醇的混合洗液"），AI自动判断危险类别并建议废弃物容器。

---

### 4.2 合规性追踪

**功能描述**：
- 生成符合EHS规范的危险废弃物标签
- 追踪废弃物从产生、暂存到收运处置的全过程
- 支持上传处置证明文件

**状态流转**：
1. `REGISTERED` - 已登记
2. `STORED` - 暂存中
3. `COLLECTED` - 已收运
4. `DISPOSED` - 已处置

**标签信息**：
```json
{
  "disposalNo": "WD20250101123456",
  "wasteType": "LIQUID",
  "mainComponents": "丙酮、乙醇",
  "quantity": 5.0,
  "unit": "L",
  "categoryName": "不含卤素有机废液",
  "hazardLevel": "MEDIUM",
  "labelColor": "ORANGE"
}
```

**AI辅助功能**：
"生成本季度所有废弃物的汇总报告，并高亮显示与EHS规定不符的操作。"

---

## 模块五：AI 智能分析与预测 (Analytics & Forecasting) 模块

### 5.1 消耗速率与智能预测

**功能描述**：
- 分析历史消耗数据，识别消耗趋势
- 基于时序预测算法预测试剂耗尽日期
- 考虑供应商到货周期，智能推荐下单日期
- 建议订购数量

**数据表**：
- `consumption_pattern` - 消耗模式分析表
- `forecasting_record` - 智能预测记录表

**核心算法**：
```java
// 计算平均日消耗量（基于最近90天）
averageDailyConsumption = totalConsumption / days

// 预测耗尽日期
daysUntilDepletion = currentStock / averageDailyConsumption
depletionDate = today + daysUntilDepletion

// 建议下单日期（提前供应商到货周期 + 缓冲时间3天）
recommendedOrderDate = depletionDate - (supplierLeadTime + 3)

// 建议订购数量（30天用量）
recommendedQuantity = averageDailyConsumption * 30
```

**置信度计算**：
基于数据量和消耗稳定性（变异系数），置信度范围50%-95%。

**API接口**：
- `POST /forecasting/generate/{reagentId}` - 为指定试剂生成预测
- `GET /forecasting/active` - 获取所有活跃预测
- `GET /forecasting/upcoming` - 获取即将需要采购的试剂

**AI提示示例**：
"根据当前实验频率，[试剂B]预计将在15天后耗尽。供应商C的平均到货周期为7天，建议您在8天内发起采购。"

---

### 5.2 实验模式识别

**功能描述**：
- 识别常见实验模式（如PCR、Western Blot、ELISA等）
- 自动归类领用记录
- 学习物料清单（BOM），辅助实验规划

**数据表**：
- `experiment_pattern` - 实验模式识别表
- `experiment_activity` - 实验活动记录表

**预设模式**：
1. PCR扩增实验
2. Western Blot蛋白印迹
3. ELISA酶联免疫
4. 细胞培养
5. DNA提取
6. 酸碱滴定

**识别示例**：
当学生同时领用"引物A"、"引物B"、"dNTP Mix"和"Taq酶"时，系统自动识别为"PCR实验"。

**AI辅助功能**：
- 学习实验室的主要实验活动
- 预测"组合耗材"的需求
- 帮助PI分析实验室研究方向

---

### 5.3 合规性审计助手

**功能描述**：
系统管理员可进行复杂查询和交叉比对。

**AI查询示例**：
- "查询上个月所有'易制毒/易制爆'试剂的领用记录"
- "将领用记录与实验员的审批日志进行交叉比对"
- "分析A课题组的试剂消耗结构"
- "对比不同供应商的试剂价格和到货周期"

**数据分析维度**：
- 按课题组统计
- 按用户统计
- 按试剂类型统计
- 按时间周期统计
- 按实验模式统计

---

## 数据库部署

### 1. 执行扩展脚本

```bash
mysql -u root -p reagent_management < database/modules_extension.sql
```

此脚本将：
- 创建20个新数据表
- 插入初始化数据（不兼容规则、废弃物类别、实验模式等）
- 扩展现有表（为`sys_user`、`reagent`、`inventory`添加新字段）

### 2. 验证表创建

```sql
USE reagent_management;
SHOW TABLES;
-- 应该看到所有新表
```

---

## 后端部署

所有后端代码已创建在 `backend/src/main/java/com/lab/reagent/` 目录下：

**实体类** (entity/)：
- 20个新实体类，对应20个数据表

**Mapper接口** (mapper/)：
- 20个Mapper接口，继承MyBatis-Plus的BaseMapper

**Service服务** (service/)：
- `GhsMsdsService.java` - GHS/MSDS服务
- `StorageCompatibilityService.java` - 存储兼容性检查服务
- `SopService.java` - SOP服务
- `ResearchGroupService.java` - 课题组服务
- `ProcurementService.java` - 采购服务
- `ReagentLifecycleService.java` - 试剂生命周期服务
- `WasteDisposalService.java` - 废弃物管理服务
- `ForecastingService.java` - 智能预测服务

**Controller接口** (controller/)：
- 8个Controller，提供RESTful API

### 编译运行

```bash
cd backend
mvn clean install
mvn spring-boot:run
```

---

## 前端部署

所有前端API接口已创建在 `frontend/src/api/` 目录下：

- `ghs.js` - GHS/MSDS API
- `sop.js` - SOP API
- `researchGroup.js` - 课题组 API
- `procurement.js` - 采购 API
- `lifecycle.js` - 生命周期 API
- `waste.js` - 废弃物 API
- `forecasting.js` - 预测 API

### 安装依赖并运行

```bash
cd frontend
npm install
npm run serve
```

---

## 核心功能演示

### 场景1：入库时检查存储兼容性

```javascript
// 前端调用
import { checkStorageCompatibility } from '@/api/ghs'

const checkResult = await checkStorageCompatibility({
  reagentId: 1,  // 浓硝酸（强氧化剂）
  locationId: 7,  // 102实验室-D1柜-1层（有机溶剂存放区）
  operatorId: userId,
  operatorName: userName
})

if (checkResult.data.blocked) {
  // 显示警告：强氧化剂与易燃品存在高危不兼容，操作被阻止
  alert(checkResult.data.warnings.join('\n'))
}
```

---

### 场景2：采购工作流

```javascript
// 1. 创建采购申请
import { createProcurement } from '@/api/procurement'

await createProcurement({
  groupId: 1,
  reagentName: '试剂A',
  quantity: 10,
  estimatedPrice: 100,
  purpose: '用于实验',
  applicantId: userId,
  applicantName: userName
})

// 2. PI审批
import { piApproveProcurement } from '@/api/procurement'

await piApproveProcurement({
  id: 1,
  reviewerId: piId,
  reviewerName: piName,
  remark: '批准',
  approved: true
})

// 3. 管理员下单
import { adminApproveProcurement } from '@/api/procurement'

await adminApproveProcurement({
  id: 1,
  reviewerId: adminId,
  reviewerName: adminName,
  remark: '已下单',
  actualPrice: 95,
  supplier: '供应商A'
})
```

---

### 场景3：扫码领用试剂

```javascript
// 扫描试剂二维码
import { scanQrCode } from '@/api/lifecycle'

const result = await scanQrCode(qrCode, userId, userName)

// 显示试剂详情
console.log(result.data)
// {
//   physicalStatus: 'SEALED',
//   remainingQuantity: 500,
//   openedDate: null,
//   ...
// }

// 首次领用，标记为已开封
import { markAsOpened } from '@/api/lifecycle'

await markAsOpened({
  qrCode: qrCode,
  userId: userId,
  userName: userName,
  validityDays: 180  // 开封后6个月有效
})
```

---

### 场景4：废弃物登记

```javascript
// 创建废弃物登记
import { createWaste } from '@/api/waste'

await createWaste({
  wasteCategoryId: 2,  // 不含卤素有机废液
  wasteType: 'LIQUID',
  sourceType: 'EXPERIMENT',
  mainComponents: '丙酮、乙醇混合',
  quantity: 5.0,
  unit: 'L',
  containerType: '25L塑料桶',
  containerCount: 1,
  applicantId: userId,
  applicantName: userName,
  groupId: groupId
})

// 生成标签
import { generateWasteLabel } from '@/api/waste'

const label = await generateWasteLabel(wasteId)
// 打印标签...
```

---

### 场景5：智能预测采购

```javascript
// 为试剂生成预测
import { generateForecast } from '@/api/forecasting'

const forecast = await generateForecast(reagentId)

console.log(forecast.data)
// {
//   reagentName: '试剂A',
//   currentStock: 50,
//   predictedDepletionDate: '2025-01-15',
//   daysUntilDepletion: 15,
//   averageDailyConsumption: 3.33,
//   recommendedOrderDate: '2025-01-05',
//   recommendedOrderQuantity: 100,
//   confidenceLevel: 85.5
// }

// 获取即将需要采购的试剂
import { getUpcomingOrders } from '@/api/forecasting'

const upcoming = await getUpcomingOrders()
// 返回建议下单日期在未来7天内的试剂列表
```

---

## AI集成建议

### 1. MSDS自动解析

利用Qwen或其他大语言模型，解析上传的MSDS PDF文件：

```python
# 伪代码示例
def extract_msds_info(pdf_file):
    text = extract_pdf_text(pdf_file)
    
    prompt = f"""
    请从以下MSDS文本中提取关键信息，以JSON格式返回：
    - GHS象形图（数组）
    - H-phrases（数组）
    - P-phrases（数组）
    - 信号词
    - 危险类别
    - 物理化学性质
    - 急救措施
    
    文本内容：
    {text}
    """
    
    response = qwen_api.generate(prompt)
    return json.loads(response)
```

---

### 2. 废弃物智能分类

根据用户描述自动判断废弃物类别：

```python
def classify_waste(description):
    prompt = f"""
    用户描述的废弃物成分如下：
    "{description}"
    
    请判断其属于以下哪个类别：
    1. 含卤素有机废液
    2. 不含卤素有机废液
    3. 无机酸性废液
    4. 无机碱性废液
    5. 重金属废液
    6. 实验固废
    7. 空试剂瓶
    8. 一般废弃物
    
    并给出建议的储存容器类型。
    """
    
    response = qwen_api.generate(prompt)
    return response
```

---

### 3. 成本分析助手

回答复杂的成本分析问题：

```python
def analyze_cost(question, data):
    prompt = f"""
    用户问题：{question}
    
    相关数据：
    {json.dumps(data)}
    
    请进行分析并给出详细回答。
    """
    
    response = qwen_api.generate(prompt)
    return response
```

---

## 性能优化建议

### 1. 数据库索引

所有外键字段已自动创建索引，对于高频查询字段建议添加复合索引：

```sql
-- 采购申请按状态和课题组查询
CREATE INDEX idx_procurement_status_group ON procurement_request(status, group_id);

-- 废弃物按状态和登记时间查询
CREATE INDEX idx_waste_status_create ON waste_disposal_record(disposal_status, create_time);

-- 预测记录按推荐下单日期查询
CREATE INDEX idx_forecast_order_date ON forecasting_record(recommended_order_date, status);
```

---

### 2. 缓存策略

对于不常变化的数据，建议使用Redis缓存：

- 不兼容规则
- 废弃物类别
- 实验模式
- SOP文档列表

---

### 3. 异步处理

耗时操作建议异步执行：

- MSDS文件解析
- 智能预测生成
- 报表生成
- 标签批量打印

---

## 安全注意事项

### 1. 权限控制

建议为新模块添加细粒度权限：

- GHS/MSDS管理权限
- SOP编辑/审核权限
- 课题组预算管理权限
- 采购审批权限（PI/管理员）
- 废弃物登记/审核权限

### 2. 敏感数据保护

- 预算数据仅课题组成员可见
- 废弃物登记记录需审计追踪
- 扫码操作全部记录日志

### 3. 操作审计

所有关键操作均已记录审计日志：

- 存储警告日志
- 扫码操作日志
- 预算交易记录
- 废弃物状态变更

---

## 未来扩展方向

### 1. 移动端PWA应用

开发基于Vue的渐进式Web应用（PWA），支持：
- 离线扫码
- 推送通知（库存预警、待审批提醒）
- 相机集成（拍照记录试剂状况）

### 2. 物联网集成

- RFID读写器集成
- 温湿度传感器监控
- 智能柜门控制

### 3. 高级AI功能

- 实验方案推荐
- 试剂替代建议
- 异常消耗检测
- 安全风险预测

### 4. 数据可视化

- 预算使用趋势图
- 消耗热力图
- 废弃物统计报表
- 实验活动分析看板

---

## 技术支持

如有问题，请参考以下资源：

1. 数据库脚本：`database/modules_extension.sql`
2. 后端代码：`backend/src/main/java/com/lab/reagent/`
3. 前端API：`frontend/src/api/`
4. 原系统文档：`README.md`、`功能清单.md`

---

## 总结

本次更新为实验室管理系统添加了五大强大模块，覆盖了实验室管理的关键环节：

✅ **安全合规** - GHS/MSDS管理、存储兼容性检查、SOP准入  
✅ **预算管理** - 课题组预算、采购工作流、成本核算  
✅ **生命周期** - 二维码追踪、状态监控、可视化库存  
✅ **废弃物** - 废液登记、合规追踪、标签生成  
✅ **智能分析** - 消耗预测、模式识别、审计助手  

系统现已具备**企业级实验室管理**的完整功能，可满足高校、科研机构、企业实验室的复杂管理需求。

---

**开发团队**  
日期：2025年11月6日  
版本：v2.0.0

