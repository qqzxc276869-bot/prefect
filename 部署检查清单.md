# 实验室管理系统 - 五大新模块部署检查清单

## ✅ 部署前检查

### 1. 数据库准备

- [ ] MySQL服务正常运行
- [ ] 数据库 `reagent_management` 已存在
- [ ] 数据库用户权限正确（root或具有完整权限的用户）
- [ ] 现有数据已备份

**验证命令**：
```bash
mysql -u root -p -e "SHOW DATABASES LIKE 'reagent_management';"
```

---

### 2. 后端环境

- [ ] JDK 8+ 已安装
- [ ] Maven 3.6+ 已安装
- [ ] `backend/src/main/resources/application.yml` 配置正确
  - 数据库连接信息
  - 端口配置（默认8080）
  - Ollama配置（如需AI功能）

**验证命令**：
```bash
java -version
mvn -version
```

---

### 3. 前端环境

- [ ] Node.js 14+ 已安装
- [ ] npm 6+ 已安装
- [ ] `frontend/vue.config.js` 代理配置正确

**验证命令**：
```bash
node -v
npm -v
```

---

## 🚀 部署步骤

### 步骤1：执行数据库脚本

#### 1.1 执行主扩展脚本

```bash
cd "C:\Users\27686\Desktop\test1 - 副本 - 副本"
mysql -u root -p reagent_management < database/modules_extension.sql
```

- [ ] 脚本执行成功，无错误提示
- [ ] 创建了20个新表
- [ ] 插入了初始化数据

**验证**：
```sql
USE reagent_management;

-- 应该看到所有新表
SHOW TABLES LIKE '%ghs%';
SHOW TABLES LIKE '%research_group%';
SHOW TABLES LIKE '%procurement%';
SHOW TABLES LIKE '%lifecycle%';
SHOW TABLES LIKE '%waste%';
SHOW TABLES LIKE '%forecasting%';

-- 验证初始化数据
SELECT COUNT(*) FROM storage_incompatibility_rules;  -- 应该 >= 7
SELECT COUNT(*) FROM waste_category;  -- 应该 = 8
SELECT COUNT(*) FROM experiment_pattern;  -- 应该 = 6
```

---

#### 1.2 执行测试数据脚本（可选）

```bash
mysql -u root -p reagent_management < database/test_modules.sql
```

- [ ] 测试数据插入成功
- [ ] 看到数据统计信息

---

### 步骤2：启动后端服务

#### 2.1 首次编译

```bash
cd backend
mvn clean install
```

- [ ] 编译成功
- [ ] 生成 `target/` 目录
- [ ] 无编译错误

**预期输出**：
```
[INFO] BUILD SUCCESS
[INFO] Total time: XX.XXX s
```

---

#### 2.2 启动服务

```bash
mvn spring-boot:run
```

- [ ] 服务启动成功
- [ ] 看到 "Started ReagentManagementApplication" 日志
- [ ] 端口8080已监听
- [ ] 无启动错误

**验证**：
打开浏览器访问：`http://localhost:8080/backend/`

或使用curl：
```bash
curl http://localhost:8080/backend/ghs/page?current=1&size=10
```

---

### 步骤3：启动前端服务

#### 3.1 安装依赖（首次）

```bash
cd frontend
npm install
```

- [ ] 依赖安装成功
- [ ] 生成 `node_modules/` 目录
- [ ] 无错误提示

---

#### 3.2 启动服务

```bash
npm run serve
```

- [ ] 服务启动成功
- [ ] 看到编译成功信息
- [ ] 端口8081已监听（或配置的端口）

**验证**：
打开浏览器访问：`http://localhost:8081`

---

## 🧪 功能测试

### 测试1：GHS/MSDS管理 ✅

**测试步骤**：
1. 调用API创建MSDS记录
2. 根据试剂ID查询MSDS
3. 验证数据正确返回

**测试命令**：
```bash
# 保存MSDS
curl -X POST http://localhost:8080/backend/ghs/save \
  -H "Content-Type: application/json" \
  -d '{
    "reagentId": 1,
    "signalWord": "Danger",
    "hazardClass": "腐蚀品",
    "ghsPictograms": "[\"GHS05\"]",
    "hPhrases": "[\"H314\"]",
    "verified": 1
  }'

# 查询MSDS
curl http://localhost:8080/backend/ghs/reagent/1
```

- [ ] MSDS保存成功
- [ ] 查询返回正确数据

---

### 测试2：存储兼容性检查 ✅

**测试步骤**：
1. 尝试将强氧化剂存入易燃品区
2. 验证系统阻止操作
3. 查看警告日志

**测试命令**：
```bash
curl -X POST http://localhost:8080/backend/storage/compatibility/check \
  -H "Content-Type: application/json" \
  -d '{
    "reagentId": 24,
    "locationId": 7,
    "operatorId": 1,
    "operatorName": "测试用户"
  }'
```

**预期结果**：
```json
{
  "code": 500,
  "message": "存储被阻止，存在高危不兼容",
  "data": {
    "blocked": true,
    "warnings": ["与现有试剂 [无水乙醇] 存在不兼容..."]
  }
}
```

- [ ] 系统正确识别不兼容
- [ ] 阻止高危操作
- [ ] 记录警告日志

**验证日志**：
```sql
SELECT * FROM storage_warning_log ORDER BY create_time DESC LIMIT 5;
```

---

### 测试3：课题组预算管理 ✅

**测试步骤**：
1. 创建课题组
2. 添加成员
3. 查看预算信息

**测试命令**：
```bash
# 创建课题组
curl -X POST http://localhost:8080/backend/research-group/save \
  -H "Content-Type: application/json" \
  -d '{
    "groupName": "测试课题组",
    "groupCode": "TEST-LAB-001",
    "piId": 2,
    "piName": "张教授",
    "totalBudget": 50000.00,
    "budgetYear": 2025,
    "status": "ACTIVE"
  }'

# 查询课题组
curl http://localhost:8080/backend/research-group/page?current=1&size=10
```

- [ ] 课题组创建成功
- [ ] 预算字段自动计算（availableBudget = totalBudget）
- [ ] 查询返回正确数据

---

### 测试4：采购工作流 ✅

**测试步骤**：
1. 创建采购申请
2. PI审批
3. 管理员下单
4. 验证预算扣减

**测试命令**：
```bash
# 1. 创建采购申请（需要先有课题组ID）
curl -X POST http://localhost:8080/backend/procurement/create \
  -H "Content-Type: application/json" \
  -d '{
    "groupId": 1,
    "reagentName": "测试试剂",
    "quantity": 5.0,
    "estimatedPrice": 100.00,
    "purpose": "测试采购流程",
    "applicantId": 4,
    "applicantName": "王同学"
  }'

# 2. PI审批（假设采购申请ID为1）
curl -X POST http://localhost:8080/backend/procurement/approve/pi \
  -H "Content-Type: application/json" \
  -d '{
    "id": 1,
    "reviewerId": 2,
    "reviewerName": "张教授",
    "remark": "批准",
    "approved": true
  }'

# 3. 管理员下单
curl -X POST http://localhost:8080/backend/procurement/approve/admin \
  -H "Content-Type: application/json" \
  -d '{
    "id": 1,
    "reviewerId": 1,
    "reviewerName": "管理员",
    "remark": "已下单",
    "actualPrice": 95.00,
    "supplier": "测试供应商"
  }'
```

- [ ] 采购申请创建成功，自动生成单号
- [ ] PI审批通过，状态变为PI_APPROVED
- [ ] 管理员下单成功，状态变为ORDERED
- [ ] 课题组预算正确扣减

**验证预算**：
```sql
SELECT 
    group_name,
    total_budget,
    used_budget,
    available_budget
FROM research_group 
WHERE id = 1;
```

**验证交易记录**：
```sql
SELECT * FROM budget_transaction WHERE group_id = 1 ORDER BY create_time DESC;
```

---

### 测试5：试剂生命周期追踪 ✅

**测试步骤**：
1. 创建生命周期记录（自动生成二维码）
2. 扫码查询
3. 标记为已开封
4. 记录使用

**测试命令**：
```bash
# 1. 创建生命周期记录
curl -X POST http://localhost:8080/backend/lifecycle/create \
  -H "Content-Type: application/json" \
  -d '{
    "inventoryId": 5,
    "reagentId": 5,
    "physicalStatus": "SEALED",
    "currentLocationId": 3,
    "remainingQuantity": 500.0
  }'

# 2. 扫码查询（使用返回的qrCode）
curl "http://localhost:8080/backend/lifecycle/scan/QR1730912350_5?userId=4&userName=王同学"

# 3. 标记为已开封
curl -X POST http://localhost:8080/backend/lifecycle/mark-opened \
  -H "Content-Type: application/json" \
  -d '{
    "qrCode": "QR1730912350_5",
    "userId": 4,
    "userName": "王同学",
    "validityDays": 180
  }'

# 4. 记录使用
curl -X POST http://localhost:8080/backend/lifecycle/record-usage \
  -H "Content-Type: application/json" \
  -d '{
    "qrCode": "QR1730912350_5",
    "usedQuantity": 50.0
  }'
```

- [ ] 二维码自动生成
- [ ] 扫码查询返回详细信息
- [ ] 开封状态更新成功
- [ ] 使用记录保存成功，剩余量更新

**验证扫码日志**：
```sql
SELECT * FROM scan_operation_log ORDER BY create_time DESC LIMIT 10;
```

---

### 测试6：废弃物登记 ✅

**测试步骤**：
1. 创建废弃物登记
2. 生成标签
3. 更新状态

**测试命令**：
```bash
# 1. 创建废弃物登记
curl -X POST http://localhost:8080/backend/waste/create \
  -H "Content-Type: application/json" \
  -d '{
    "wasteCategoryId": 2,
    "wasteType": "LIQUID",
    "sourceType": "EXPERIMENT",
    "mainComponents": "测试废液",
    "quantity": 2.0,
    "unit": "L",
    "containerType": "10L玻璃瓶",
    "containerCount": 1,
    "applicantId": 4,
    "applicantName": "王同学",
    "groupId": 1,
    "storageLocation": "废液暂存间"
  }'

# 2. 生成标签（假设废弃物ID为1）
curl http://localhost:8080/backend/waste/label/1

# 3. 更新状态
curl -X POST http://localhost:8080/backend/waste/update-status \
  -H "Content-Type: application/json" \
  -d '{
    "id": 1,
    "status": "STORED",
    "remark": "已移至废液暂存间"
  }'
```

- [ ] 废弃物登记成功，自动生成单号
- [ ] 标签信息生成正确
- [ ] 状态更新成功

**验证废弃物记录**：
```sql
SELECT 
    disposal_no,
    wc.category_name,
    waste_type,
    main_components,
    disposal_status,
    label_printed
FROM waste_disposal_record wd
LEFT JOIN waste_category wc ON wd.waste_category_id = wc.id
ORDER BY create_time DESC
LIMIT 5;
```

---

### 测试7：智能预测 ✅

**测试步骤**：
1. 为试剂生成预测
2. 查看预测结果
3. 查询即将需要采购的试剂

**前提条件**：
试剂需要有足够的历史出库记录（至少3条）

**测试命令**：
```bash
# 1. 生成预测（试剂ID 1 - 浓盐酸）
curl -X POST http://localhost:8080/backend/forecasting/generate/1

# 2. 查看所有活跃预测
curl http://localhost:8080/backend/forecasting/active

# 3. 查询即将需要采购的试剂
curl http://localhost:8080/backend/forecasting/upcoming
```

- [ ] 预测生成成功
- [ ] 包含预计耗尽日期、建议下单日期、建议数量等信息
- [ ] 置信度计算合理

**如果数据不足**：
可以先插入一些测试出库记录：
```sql
-- 插入测试出库记录（最近90天内）
INSERT INTO stock_out_record 
(reagent_id, inventory_id, quantity, recipient_id, recipient_name, operator_id, operator_name, purpose, create_time)
SELECT 
    1, 1, 
    3.0 + RAND() * 2,  -- 随机数量 3-5
    4, '王同学', 2, '张教授',
    '实验使用',
    DATE_SUB(NOW(), INTERVAL FLOOR(RAND() * 90) DAY)
FROM 
    (SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5) t;

-- 然后再生成预测
```

---

### 测试8：SOP管理 ✅

**测试步骤**：
1. 创建SOP文档
2. 查询SOP列表
3. 检查培训状态

**测试命令**：
```bash
# 1. 创建SOP
curl -X POST http://localhost:8080/backend/sop/save \
  -H "Content-Type: application/json" \
  -d '{
    "title": "测试SOP",
    "sopNo": "SOP-TEST-001",
    "version": "1.0",
    "category": "测试",
    "content": "这是一个测试SOP",
    "applicableReagents": "[1, 2, 3]",
    "applicableHazardTypes": "[\"腐蚀品\"]",
    "status": "ACTIVE",
    "authorId": 2,
    "authorName": "张教授"
  }'

# 2. 查询SOP列表
curl "http://localhost:8080/backend/sop/page?current=1&size=10&status=ACTIVE"

# 3. 检查培训状态
curl "http://localhost:8080/backend/sop/training/check?userId=4&sopId=1"
```

- [ ] SOP创建成功
- [ ] 查询返回正确列表
- [ ] 培训状态检查正常

---

## 📊 数据完整性检查

执行以下SQL验证数据完整性：

```sql
USE reagent_management;

-- 1. 检查所有新表是否存在
SELECT 
    TABLE_NAME,
    TABLE_ROWS,
    CREATE_TIME
FROM information_schema.TABLES
WHERE TABLE_SCHEMA = 'reagent_management'
  AND TABLE_NAME IN (
    'ghs_msds',
    'storage_location_attributes',
    'storage_incompatibility_rules',
    'storage_warning_log',
    'sop_document',
    'sop_training_record',
    'research_group',
    'research_group_member',
    'procurement_request',
    'budget_transaction',
    'reagent_lifecycle',
    'storage_location_map',
    'storage_location_coordinate',
    'scan_operation_log',
    'waste_category',
    'waste_disposal_record',
    'consumption_pattern',
    'forecasting_record',
    'experiment_pattern',
    'experiment_activity'
  )
ORDER BY TABLE_NAME;

-- 2. 检查初始化数据
SELECT '不兼容规则' AS type, COUNT(*) AS count FROM storage_incompatibility_rules WHERE enabled = 1
UNION ALL
SELECT '废弃物类别', COUNT(*) FROM waste_category
UNION ALL
SELECT '实验模式', COUNT(*) FROM experiment_pattern;

-- 3. 检查外键关系（示例）
SELECT 
    COUNT(*) AS orphaned_records
FROM ghs_msds g
LEFT JOIN reagent r ON g.reagent_id = r.id
WHERE r.id IS NULL;
-- 应该返回 0

-- 4. 检查JSON字段格式
SELECT 
    id,
    ghs_pictograms,
    JSON_VALID(ghs_pictograms) AS is_valid_json
FROM ghs_msds
WHERE ghs_pictograms IS NOT NULL
LIMIT 5;
```

- [ ] 所有20个新表都存在
- [ ] 初始化数据正确（7条规则、8个类别、6个模式）
- [ ] 无孤立记录（外键完整性）
- [ ] JSON字段格式正确

---

## 🔍 性能检查

### 查询性能测试

```sql
-- 1. 检查索引是否创建
SHOW INDEX FROM ghs_msds WHERE Key_name != 'PRIMARY';
SHOW INDEX FROM procurement_request WHERE Key_name != 'PRIMARY';
SHOW INDEX FROM reagent_lifecycle WHERE Key_name != 'PRIMARY';

-- 2. 执行慢查询测试
EXPLAIN SELECT * FROM procurement_request WHERE status = 'PENDING' AND group_id = 1;
EXPLAIN SELECT * FROM reagent_lifecycle WHERE qr_code = 'QR1730912345_1';
EXPLAIN SELECT * FROM waste_disposal_record WHERE disposal_status = 'REGISTERED';
```

- [ ] 关键字段已创建索引
- [ ] EXPLAIN显示使用了索引（type = ref 或 const）
- [ ] 查询速度正常

---

## 🔐 安全检查

### API安全测试

1. **CORS配置**：
   - [ ] 跨域请求正常
   - [ ] CorsConfig.java 配置正确

2. **SQL注入防护**：
   - [ ] 使用MyBatis-Plus的参数化查询
   - [ ] 无直接字符串拼接SQL

3. **权限控制**（未来扩展）：
   - [ ] 计划添加权限控制
   - [ ] 敏感操作需要权限验证

---

## 📝 文档检查

- [ ] `新模块功能说明.md` - 详细功能文档
- [ ] `快速开始指南.md` - 部署指南
- [ ] `部署检查清单.md` - 本文档
- [ ] `database/modules_extension.sql` - 数据库扩展脚本
- [ ] `database/test_modules.sql` - 测试数据脚本

---

## 🎉 最终验证

### 完整功能流程测试

**场景：学生完整的试剂使用流程**

1. ✅ 学生申请领用试剂
2. ✅ 系统检查SOP培训（如果是高危试剂）
3. ✅ 老师审批领用申请
4. ✅ 系统检查存储兼容性
5. ✅ 学生扫码领用（生成二维码记录）
6. ✅ 首次开封，标记状态
7. ✅ 使用完毕，记录使用量
8. ✅ 产生废液，提交废弃物登记
9. ✅ 系统自动预测库存，提醒采购
10. ✅ 老师发起采购申请
11. ✅ PI审批
12. ✅ 管理员下单，扣减预算

---

## 🚨 常见问题排查

### 问题1：数据库脚本执行失败

**错误信息**：`Table 'xxx' already exists`

**解决方案**：
```sql
-- 删除已存在的表
DROP TABLE IF EXISTS ghs_msds;
DROP TABLE IF EXISTS storage_location_attributes;
-- ... 其他表

-- 然后重新执行脚本
```

---

### 问题2：后端启动失败

**错误信息**：`Could not create connection to database`

**排查步骤**：
1. 检查MySQL服务是否运行
2. 检查`application.yml`中的数据库连接配置
3. 测试数据库连接：
   ```bash
   mysql -u root -p reagent_management -e "SELECT 1"
   ```

---

### 问题3：API返回500错误

**排查步骤**：
1. 查看后端控制台日志
2. 检查数据库表是否存在
3. 检查请求参数格式
4. 使用Postman测试API

---

### 问题4：前端无法连接后端

**排查步骤**：
1. 检查后端服务是否启动（访问 http://localhost:8080/backend/）
2. 检查`vue.config.js`中的代理配置
3. 查看浏览器控制台Network选项卡
4. 检查CORS配置

---

## 📋 部署完成确认

完成以下所有检查后，即可认为部署成功：

- [ ] ✅ 数据库脚本执行成功，20个新表已创建
- [ ] ✅ 初始化数据正确插入（规则、类别、模式）
- [ ] ✅ 后端服务正常启动，无错误日志
- [ ] ✅ 前端服务正常启动，可访问页面
- [ ] ✅ 8个核心功能测试全部通过
- [ ] ✅ API接口返回正确数据
- [ ] ✅ 数据完整性检查通过
- [ ] ✅ 性能检查正常
- [ ] ✅ 文档齐全

---

## 🎊 恭喜！部署成功！

您已成功部署实验室管理系统的五大新模块：

1. ✅ **安全与合规模块** - GHS/MSDS、存储兼容性、SOP管理
2. ✅ **采购与预算模块** - 课题组管理、采购工作流、成本核算
3. ✅ **全生命周期追踪模块** - 二维码追踪、状态监控、可视化库存
4. ✅ **废弃物管理模块** - 废液登记、合规追踪、标签生成
5. ✅ **AI智能分析模块** - 消耗预测、模式识别、审计助手

---

**下一步**：
1. 查看 `快速开始指南.md` 了解使用方法
2. 查看 `新模块功能说明.md` 了解完整功能
3. 根据实际需求定制前端界面
4. 集成AI服务（Ollama/Qwen）
5. 配置权限系统

**技术支持**：
如有问题，请参考文档或联系开发团队。

---

**祝使用愉快！** 🎉🎊🚀

